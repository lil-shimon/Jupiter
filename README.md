```
docker-compose build
docker-compose up -d


//起動したコンテナに入る
docker exec -it python_machine_learning /bin/sh -c "[ -e /bin/bash ] && /bin/bash || /bin/sh"

option
tensorflow
pip3 install --upgrade https://storage.googleapis.com/tensorflow/mac/cpu/tensorflow-1.8.0-py3-none-any.whl

matplotlib
tensorflow
keras
gensim
はrequirements.txtに追記予定
```
